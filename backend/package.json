{
  "name": "@category-bomb/server",
  "version": "1.0.0",
  "description": "Category Bomb Arena - Backend Server",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "build": "bun build src/index.ts --outdir ./dist --target bun",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "game",
    "websocket",
    "realtime",
    "web3"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@next/swc-win32-x64-msvc": "^16.1.6",
    "ethers": "^6.10.0",
    "hono": "^4.0.5",
    "nanoid": "^5.0.5"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
